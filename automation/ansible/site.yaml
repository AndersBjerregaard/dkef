- name: Configure systems with Docker, Kind and Kubectl
  hosts: hosts

  vars_files:
    - vars/secrets.yaml

  roles:
    - role: kubernetes_python
      tags: kubernetes_python
    - role: dkef_cluster
      tags: dkef_cluster
    - role: postgres_k8s
      tags: postgres_k8s
    - role: frontend_k8s
      tags: frontend_k8s