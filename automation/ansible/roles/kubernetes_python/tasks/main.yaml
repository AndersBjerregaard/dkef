- name: Install pip3
  ansible.builtin.apt:
    name: python3-pip
    state: present
    update_cache: yes
  become: yes

- name: Install kubernetes Python library
  ansible.builtin.pip:
    name: kubernetes
    state: present
    extra_args: --break-system-packages
  become: yes